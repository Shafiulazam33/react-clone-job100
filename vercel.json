{
  "version": 2,
  "builds": [
    {
      "src": "/index.js",
      "use": "@vercel/node"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",

      "Methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "Access-Control-Allow-Origin": "*"
    }
  ]
}
